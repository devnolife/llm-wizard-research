[pytest]
testpaths = tests
python_files = test_*.py *_test.py
python_classes = Test*
python_functions = test_*
pythonpath = .
asyncio_mode = auto
markers =
    unit: isolated unit tests
    integration: component integration tests
    api: FastAPI contract tests with mocked dependencies
    ocr: OCR pipeline tests
    slow: tests requiring heavyweight resources
